# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент, за отображение которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

### Данные
Данные, которые будут использоваться в приложении, - данные о товаре и данные о покупателе.

#### Интерфейс IProduct
```
interface IProduct {  
  id: string;            //уникальный идентификатор  
  description: string;   //полное описание товара  
  image: string;         //изображение товара  
  title: string;         //наименование товара  
  category: string;      //категория, к которой относится товар  
  price: number | null;  //стоимость товара  
}  
```  
 Предназначен для учета товаров, которые будут использоваться в приложении.
 Описывает свойства товара и определяет их типы данных, чтобы соответствовать данным, приходящим с сервера.

#### Интерфейс IBuyer
```
interface IBuyer {  
  payment: TPayment;  //способ оплаты товара  
  email: string;      //емэйл покупателя  
  phone: string;      //номер телефона покупателя  
  address: string;    //адрес доставки товар(а/ов)  
}  
```  
 Предназначен для работы с данными покупателя, которые будут использоваться в приложении.  
 Описывает свойства данных о покупателе и определяет их типы. Тем самым интерфейс помогает контролировать, какие данные нужно собрать от покупателя и передать на сервер.

### Модели данных
Классы, которые отвечают за хранение и работу с данными.

#### Класс Products
Класс создан для хранения товаров, которые можно купить в приложении.
Управляет товарами, которые являются объектами интерфейса IProduct. Сохраняет полученные от сервера данные о товарах и отдельно выбранный покупателем товар, с возможностью дальнейшего пользования этими данными, в том числе получение одного товара по его идентификатору.

Конструктор:  
`constructor(protected events: IEvents)` - В конструктор передается брокер событий, который связывает модель данных с остальным приложением. Модель с помощью брокера генерирует события, не зная, кто и как на них отреагирует.  

Поля класса:  
`private products: IProduct[] = []` - поле для хранения массива всех товаров  
`private product: IProduct | null = null` - поле для хранения товара, выбранного для подробного отображения  

Методы класса:  
`set productsSet(products: IProduct[]): void`- сохранение массива товаров, полученного в параметрах метода  
`get productsGet(): IProduct[]` - получение массива товаров из модели  
`getProductById(id: string): IProduct | undefined` - получение одного товара по его id, принимаемого в параметре метода  
`set productSet(product: IProduct | null): void` - сохранение выбранного товара, полученного в параметре метода, для подробного отображения  
`get productGet(): IProduct | null` - получение выбранного товара для подробного отображения  

#### Класс Cart
Класс создан для хранения товаров, которые пользователь выбрал для покупки. 
Управляет товарами, которые соответствуют интерфейсу IProduct. Дает возможность добавления и удаления товара из корзины, проведения ее очистки, получения общего количества товаров в корзине и подсчета их итоговой стоимости. Позволяет проверить по идентификатору товара его наличие в корзине.

Конструктор:  
`constructor(protected events: IEvents)` - В конструктор передается брокер событий, который связывает модель данных с остальным приложением. Модель с помощью брокера генерирует события, не зная, кто и как на них отреагирует.  

Поля класса:  
`private cartProducts: IProduct[] = []` - поле для хранения массива товаров, выбранных покупателем для покупки  

Методы класса:  
`get cartProductsGet(): IProduct[]` - получение массива товаров, которые находятся в корзине  
`addProductToCart(product: IProduct): void` - добавление товара, который был получен в параметре, в массив корзины  
`deleteCartProduct(product: IProduct): void` - удаление товара, полученного в параметре, из массива корзины  
`clearCart(): void` - очистка корзины  
`getTotalCost(): number` - получение стоимости всех товаров в корзине  
`getTotalQuantity(): number` - получение количества товаров в корзине  
`checkProductById(id: string): boolean` - проверка наличия товара в корзине по его id, полученному в параметре метода. Позволяет избежать дублирования

#### Класс Buyer
Содержит данные покупателя, которые он должен указать при оформлении заказа. Управляет данными, которые соответствуют интерфейсу IBuyer. Дает возможность работать как со всеми данными вместе, так и по отдельности, а также их очищать и проводить валидацию для выявления ошибок.  

Конструктор:  
`constructor(protected events: IEvents)` - В конструктор передается брокер событий, который связывает модель данных с остальным приложением. Модель с помощью брокера генерирует события, не зная, кто и как на них отреагирует.  

Поля класса:  
`private payment: TPayment = ""` - поле для хранения данных о виде оплаты. Тип данных представляет собой объединение из строк "card", "cash" и пустой строки  
`private email: string = ""` - поле для хранения данных о email  
`private phone: string = ""` - поле для хранения данных о номере телефона  
`private address: string = ""` - поле для хранения данных об адреcе доставки  

Методы класса:  
`set paymentSet(value: TPayment): void` - сохранение данных о виде оплаты, которые передаются в параметре метода, в модели  
`set emailSet(value: string): void` - сохранение данных о email в модели  
`set phoneSet(value: string): void` - сохранение данных  о номере телефона в модели  
`set addressSet(value: string): void` - сохранение данных об адреcе в модели  
`get buyerData(): IBuyer` - получение всех данных покупателя в виде объекта, соответствующего интерфейсу IBuyer  
`clearBuyerData(): void` - очистка данных покупателя  
`validateBuyerData(): IBuyerValidateErr` - метод, который проверяет данные покупателя на валидность и возвращает результат этой проверки в виде объекта, соответствующего интерфейсу IBuyerValidateErr. В объекте присутствуют поля, соответствующие полям класса, значениями у которых является текст ошибки. Если поле не содержит ошибок, то такое свойство в объекте отсутствует

### Слой коммуникации

#### Класс ApiCommunication
 Класс является представителем коммуникационного слоя — он отвечает за получение данных с сервера и отправку данных на сервер. Класс использует композицию, чтобы выполнить запрос на сервер с помощью метода get класса Api и получает с сервера объект с массивом товаров. С помощью метода post класса Api данный класс отправляет на сервер данные о покупателе и выбранных товарах.  

 Конструктор:  
 `constructor(apiObj: IApi)` - В конструктор передается объект, соответствующий интерфейсу IApi, который содержит  get и post запросы к серверу

 Поля класса:  
 `private api: IApi` - поле для хранения объекта, реализующего интерфейс IApi. Объект будет использоваться для выполнения запросов к серверу.

 Методы класса:  
`getProducts(): Promise<IProductsResponse>` - метод, который делает get запрос на эндпоинт /product/ и возвращает объект, полученный от сервера, в котором находится массив товаров  
`sendOrder(data: IOrderData): Promise<IOrderConfirm>` - метод, который делает post запрос  на эндпоинт /order/ и передаёт в него данные, полученные в параметрах метода, а возвращает объект, подтверждающий покупку на определенную сумму  

### Слой Представления (View)

#### Класс Header

Класс отвечает за представление блока с шапкой сайта. Является наследником класса Component и реализует интерфейс IHeader.  
```
interface IHeader {  
  counter: number;  //счетчик товаров в корзине  
}  
```  
 Конструктор:  
 `constructor(container: HTMLElement, protected events: IEvents)` - В конструктор передается HTML элемент, отвечающий за блок кода, который представляет класс и брокер событий, который связывает компонент с остальным приложением. Компонент с помощью брокера генерирует события, не зная, кто и как на событие отреагирует.  

 Поля класса:  
 `protected basketButton: HTMLButtonElement` - поле для хранения HTML элемента кнопки, при нажатии на которую открывается корзина товаров.  
 `protected counterElement: HTMLElement` - поле для хранения HTML элемента (счетчика), отображающего количество товаров в корзине.  

 Методы класса:  
`set counter(value: number): void` - метод, который меняет значение счетчика товаров в корзине  

#### Класс Gallery

Класс отвечает за отображение списка/галереи карточек товаров. Является наследником класса Component и реализует интерфейс IGalleryData.  
```
interface IGalleryData {  
  catalog: HTMLElement[];  //каталог карточек товаров  
}  
```  
 Конструктор:  
 `constructor(container: HTMLElement)` - В конструктор передается HTML элемент, отвечающий за блок кода, который представляет класс.  

 Методы класса:  
`set catalog(items: HTMLElement[]): void` - метод, который добавляет массив карточек товаров в галерею.  

#### Класс Modal

Класс отвечает за отображение модального окна без его содержимого, которое может потом в него вставляться. Является наследником класса Component и реализует интерфейс IModalData.  
```
interface IModalData {  
  content: HTMLElement;  //элемент - содержимое модального окна 
}  
```  
 Конструктор:  
 `constructor(container: HTMLElement)` - В конструктор передается HTML элемент, отвечающий за блок кода, который представляет класс.  

 Поля класса:  
 `protected modalButton: HTMLButtonElement` - поле для хранения HTML элемента кнопки, отвечающей за закрытие модального окна.  
 `protected modalContent: HTMLElement` - поле для хранения HTML элемента, внутрь которого можно разместить элемент - содержимое блока модального окна.  

 Методы класса:  
`set content(value: HTMLElement): void` - метод, который устанавливает HTML элемент, т.е. содержимое, внутрь модального окна.  
`open(content: HTMLElement): void` - метод, который открывает модальное окно и устанавливает при помощи метода set content элемент с содержимым.  
`close(): void` - метод, который закрывает модальное окно и очищает его от содержимого.  

#### Класс OrderSuccess

Класс отвечает за отображение окна, подтверждающего оформление заказа. Является наследником класса Component и реализует интерфейс IOrderSuccess.  
```
interface IOrderSuccess {  
  totalPrice: number;  //итоговая сумма списания 
}  
```  
 Конструктор:  
 `constructor(container: HTMLElement, protected events: IEvents)` - В конструктор передается HTML элемент, отвечающий за блок кода, который представляет класс и брокер событий, который связывает компонент с остальным приложением. Компонент с помощью брокера генерирует события, не зная, кто и как на событие отреагирует.  

 Поля класса:  
 `protected totalElement: HTMLElement` - поле для хранения HTML элемента, содержащего информацию о списанной сумме.  
 `protected successButton: HTMLButtonElement` - поле для хранения HTML элемента кнопки, которая возвращает к галерее товаров.  

 Методы класса:  
`set totalPrice(value: number): void` - метод, который устанавливает итоговую сумму покупки в поле totalElement.  

#### Класс Basket

Класс отвечает за отображение корзины товаров. Является наследником класса Component и реализует интерфейс IBasket.  
```
interface IBasket {  
  cardList: HTMLElement[];  //массив карточек товаров 
  basketTotal: number;  //итоговая стоимость товаров 
  disabledProcessButton: boolean;  //активировать/заблокировать кнопку 
}  
```  
 Конструктор:  
 `constructor(container: HTMLElement, protected events: IEvents)` - В конструктор передается HTML элемент, отвечающий за блок кода, который представляет класс и брокер событий, который связывает компонент с остальным приложением. Компонент с помощью брокера генерирует события, не зная, кто и как на событие отреагирует.  

 Поля класса:  
 `protected cardListElement: HTMLUListElement` - поле для хранения HTML элемента списка товаров в корзине.  
 `protected processButton: HTMLButtonElement` - поле для хранения HTML элемента кнопки, которая открывает модальное окно с формой оформления товара.  
 `protected basketPriceElement: HTMLElement` - поле для хранения HTML элемента, содержащего информацию об итоговой стоимости всех товаров в корзине.  

 Методы класса:  
`set cardList(items: HTMLElement[]): void` - метод, который добавляет массив карточек товаров в cardListElement корзины.  
`set basketTotal(value: number): void` - метод, который устанавливает итоговую стоимость всех выбранных товаров в поле basketPriceElement.  
`set disabledProcessButton(disabled: boolean): void` - метод, который делает кнопку активной или блокирует ее.  

#### Класс Card

Класс является абстрактным и родительским для классов FullCard и BasketCard, а также общим для всех классов с карточками. Содержит в себе общий функционал карточек товаров из дочерних классов. Является наследником класса Component и использует тип данных TCard.  
```
type TCard = Pick<IProduct, 'title' | 'price'>
```  
Конструктор:  
 `constructor(container: HTMLElement)` - В конструктор передается корневой DOM-элемент компонента, который, в свою очередь, передаётся в конструктор при создании экземпляров дочерних классов.  

 Поля класса:  
 `protected titleElement: HTMLElement` - поле для хранения HTML элемента, содержащего название товара.  
 `protected priceElement: HTMLElement` - поле для хранения HTML элемента, содержащего стоимость товара.  

 Методы класса:  
`set title(value: string): void` - метод, который устанавливает название товара для карточки.  
`set price(value: number): void` - метод, который устанавливает стоимость товара для карточки.  

#### Класс FullCard

Класс является абстрактным и родительским для классов GalleryCard и PreviewCard. Содержит в себе общий функционал для карточки из галереи и карточки превью из модального окна. Является наследником класса Card и использует типы данных CategoryKey и TFullCard.  
```
type CategoryKey = keyof typeof categoryMap;  //содержит названия категорий товаров  
type TFullCard = Pick<IProduct, 'category' | 'image'>;
```  
Конструктор:  
 `constructor(container: HTMLElement)` - В конструктор передается корневой DOM-элемент компонента, который, в свою очередь, передаётся в конструктор при создании экземпляров дочерних классов.  

 Поля класса:  
 `protected categoryElement: HTMLElement` - поле для хранения HTML элемента, содержащего название категории, к которой относится товар.  
 `protected imageElement: HTMLImageElement` - поле для хранения HTML элемента изображения, в котором находятся данные об этом изображении.  

 Методы класса:  
`set category(value: string): void` - метод, который устанавливает название категории товара и добавляет модификаторы для корректного отображения фона категорий.  
`set image(value: string): void` - метод, который устанавливает адрес изображения для отображения картинки в карточке.  

#### Класс GalleryCard

Класс наследуется от класса FullCard. Отвечает за отображение карточки товара в каталоге товаров на главной странице сайта. Использует наследуемый тип данных TFullCard.  

 Конструктор:  
 `constructor(container: HTMLElement, actions?: ICardActions)` - В конструктор передается HTML элемент, отвечающий за блок кода, который представляет класс и обработчик (функция) onClick, она эмитит событие в презентере, и вызывается по клику.  

 Поля класса:  
Поля класса наследуются от родительских классов.

 Методы класса:  
Методы наследуются от родительских классов.    

#### Класс PreviewCard

Класс наследуется от класса FullCard. Отвечает за отображение просмотра товара в модальном окне, где показана детальная информация о товаре. Реализует интерфейс IPreviewCard и использует тип данных TPreviewCard.  
```
type TPreviewCard = Pick<IProduct, 'description' | 'price'>;
interface IPreviewCard {  
  buttonText: string;  //текст кнопки в карточке превью
}  
```  
 Конструктор:  
 `constructor(container: HTMLElement, protected events: IEvents)` - В конструктор передается HTML элемент, отвечающий за блок кода, который представляет класс и брокер событий, который связывает компонент с остальным приложением. Компонент с помощью брокера генерирует события, не зная, кто и как на событие отреагирует.  

 Поля класса:  
 `protected descriptionElement: HTMLElement` - поле для хранения HTML элемента, содержащего подробное описание товара в карточке.  
 `protected cardButton: HTMLButtonElement` - поле для хранения HTML элемента кнопки, которая либо добавляет товар в корзину, либо удаляет его из корзины, или указывает на невозможность покупки. Также после нажатия на кнопку закрывается модальное окно.  

 Методы класса:  
`set description(value: string): void` - метод, который добавляет детальное описание товара для карточки.  
Переопределяется сеттер price.  
`set buttonText(text: string): void` - метод, который устанавливает текст на кнопке карточки.  

#### Класс BasketCard

Класс наследуется от класса Card. Отвечает за отображение карточки товара в корзине. Реализует интерфейс IBasketCard.  
```
interface IBasketCard {  
  index: number;  //порядковый номер карточки товара в списке корзины (индекс)
}  
```  
 Конструктор:  
 `constructor(container: HTMLElement, actions?: ICardActions)` - В конструктор передается HTML элемент, отвечающий за блок кода, который представляет класс и обработчик (функция) onClick, она эмитит событие в презентере, и вызывается по клику.  

 Поля класса:  
 `protected cardIndexElement: HTMLElement` - поле для хранения HTML элемента, содержащего порядковый номер карточки товара в списке корзины.  
 `protected deleteButton: HTMLButtonElement` - поле для хранения HTML элемента кнопки, которая удаляет карточку товара из корзины.  

 Методы класса:  
`set index(value: number): void` - метод, который устанавливает порядковый номер карточки товара в списке корзины.  

#### Класс Form

Класс является абстрактным и родительским для классов ContactsForm и OrderForm. Содержит в себе общий функционал форм для заполнения данными, необходимыми для оформления товара. Является наследником класса Component и реализует интерфейс IForm.  
```
interface IForm {    
  disabledButton: boolean;  //активная/заблокированная кнопка  
  error?: string;  //необязательное поле с ошибкой заполнения  
}  
```  
Конструктор:  
 `constructor(container: HTMLElement, protected events: IEvents)` - В конструктор передается корневой DOM-элемент компонента, который, в свою очередь, передаётся в конструктор при создании экземпляров дочерних классов. Передается  брокер событий, который связывает компонент с остальным приложением. Компонент с помощью брокера генерирует события, не зная, кто и как на событие отреагирует.  

 Поля класса:  
 `protected submitButton: HTMLButtonElement` - поле для хранения HTML элемента кнопки, отправляющей данные о заказе на сервер.  
 `protected errorElement: HTMLElement` - поле для хранения HTML элемента, содержащего текст ошибки заполения формы.  
 ` protected inputElements: HTMLInputElement[]` - поле для хранения массива HTML элементов инпутов формы, необходимых для установки на каждый из них слушателя событий.  

 Методы класса:  
`set disabledButton(disabled: boolean): void` - метод, который делает кнопку активной или блокирует ее.  
`set error(message: string): void` - метод, который добавляет текст ошибки, если поле не заполнено.  

#### Класс OrderForm

Класс наследуется от класса Form. Отвечает за отображение формы, заполняемой данными о способе оплаты товар(а/ов) и об адресе доставки. Реализует интерфейс IOrderForm и использует тип данных TOrderForm.  
```
interface IOrderForm {  
  type TOrderForm = Pick<IBuyer, 'address'>;
  selectedButton: NonEmptyPayment;  //кнопка с выбранным видом оплаты
}  
```  
 Конструктор:  
 `constructor(container: HTMLElement, events: IEvents)` - В конструктор передается HTML элемент, отвечающий за блок кода, который представляет класс и брокер событий, который связывает компонент с остальным приложением. Компонент с помощью брокера генерирует события, не зная, кто и как на событие отреагирует.  

 Поля класса:  
 `protected addressInput: HTMLInputElement` - поле для хранения HTML элемента инпута формы, отвечающего за адрес доставки.  
 `protected orderButtons: HTMLButtonElement []` - поле для хранения массива HTML элементов кнопок, которые отвечают за вид оплаты.  

 Методы класса:  
`set address(value: string): void` - метод, который устанавливает адрес доставки товара в инпут.  
`set selectedButton(method: NonEmptyPayment): void` - метод, который устанавливает модификатор для выделения кнопки с выбранным видом оплаты.  

#### Класс ContactsForm

Класс наследуется от класса Form. Отвечает за отображение формы, заполняемой данными об емэйл адресе покупателя и о номере телефона. Использует тип данных TContactsForm.  
```
type TContactsForm = Pick<IBuyer, 'email' | 'phone'>  
```  
 Конструктор:  
 `constructor(container: HTMLElement, events: IEvents)` - В конструктор передается HTML элемент, отвечающий за блок кода, который представляет класс и брокер событий, который связывает компонент с остальным приложением. Компонент с помощью брокера генерирует события, не зная, кто и как на событие отреагирует.  

 Поля класса:  
 `protected emailInput: HTMLInputElement` - поле для хранения HTML элемента инпута формы, отвечающего за емэйл адрес покупателя.  
 `protected phoneInput: HTMLInputElement` - поле для хранения HTML элемента инпута формы, отвечающего за номере телефона покупателя.  

 Методы класса:  
`set email(value: string): void` - метод, который устанавливает емэйл покупателя в инпут.  
`set phone(value: string): void` - метод, который устанавливает номер телефона покупателя в инпут.  

### События в приложении и их описание

В разделе описываются события, которые брокер событий генерирует в приложении. Они помогают Модели данных и слою Представления без прямой зависимости друг от друга уведомлять о том, что нужно изменить данные или отображение.  

События слоя Представления:  
- basket:open — нажатие кнопки открытия корзины
- orderSuccess:close — нажатие кнопки в окне подтверждения успешного оформления заказа для перехода к каталогу товаров на главной странице сайта
- basket:makeOrder — нажатие кнопки в корзине для перехода к форме оформления заказа
- basket:cardDeleted — нажатие кнопки удаления товара из корзины
- card:selected — выбор карточки товара для детального просмотра
- card:add — нажатие кнопки покупки/удаления товара в превью
- orderButton:click — нажатие на кнопку способа оплаты товар(а/ов)
- order:submit — нажатие кнопки перехода ко второй форме оформления заказа
- order:input — изменение данных в инпуте адреса в форме Order
- contacts:submit — нажатие на кнопку оплаты (завершение оформления заказа). Переход к окну подтверждения покупки и отправка данных заказа на сервер
- contacts:input — изменение данных в инпуте емэйл или телефон в форме Contacts

События Модели данных:  
- products:changed — изменение каталога товаров. Внутри него при клике на карточку эмитится событие card:selected
- selectProduct:changed — изменение выбранного для просмотра товара
- basket:changed — изменение содержимого корзины. Внутри при клике на кнопку удаления товара из корзины эмитится событие basket:cardDeleted
- buyerData:changed — изменение данных покупателя

### Презентер

Слой презентера отвечает за логику работы сайта. Так как у приложения только одна, главная, страница, то используется один презентер, реализованный в основном скрипте приложения main.ts. 
Код презентера составляют только обработчики событий, которые были сгенерированы в моделях данных и представлениях. Презентер связывает самостоятельные компоненты: то, что видит пользователь (View) и то, какие данные используются в приложении (Model). Показывает данные пользователю и реагирует на его действия.  
Логика:  
Пользователь взаимодействует с интерфейсом (эмитится событие в Представлении) -> Презентер обрабатывает событие от Представления, сохраняя или изменяя данные -> Данные меняются и Модель эмитит событие -> Срабатывает слушатель в Презентере и перерисовывается отображение с новыми данными.  

